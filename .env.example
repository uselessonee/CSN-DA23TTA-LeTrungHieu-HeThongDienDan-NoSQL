# Copy this file to .env and update values with your real credentials
# Example MongoDB URI (replace <username>, <password>, and <dbname> with your values):
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority
